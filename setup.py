from setuptools import setup, find_packages

setup(
    name='password_manager_client',
    version='0.0.1',
    description='A basic client for a basic password manager',
    author='Simon Bonnefoy',
    author_email='',
    packages=find_packages()
)

